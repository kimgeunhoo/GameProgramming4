#pragma once

#include <vector>
#include <algorithm>

class Monsters
{

};

class MonsterDatabase
{
private:
	std::vector<Monsters> monsters;
	std::vector<int> nums;
public:
	void FindMonsterById(int id)
	{
		//std::find_if();
	}
	void FindNumInDatabase(int x)
	{
		//std::find(nuns.begin(), nums.end(),x );
	}

};